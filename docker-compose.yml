version: '2.2'
services: 
    phpfpm: 
        image: phpdockerio/php73-fpm
        working_dir: /app
        volumes:
            - ${DIRECTORY}:/app
        command: /bin/bash -c "apt-get update && apt-get install -y php-ds php-mbstring && /usr/sbin/php-fpm7.3 -O"