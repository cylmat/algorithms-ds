{
    "name": "freeonisland/algorithms-ds",
    "description": "Algorithms, DS et design patterns",
    "type": "tests",
    "license": "none",
    "authors": [
        {
            "name": "freeonisland",
            "email": "freeonisland@yahoo.fr"
        }
    ],
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "stable",
    "require": {
        "mikecao/flight": "^1.3"
    },
    "scripts": {
        "try":  "php -f",
        "valid":  "php -f src/validate.php",
        "python": "docker run -d -it -v /WEBSERVER/algorithms-ds:/appli -w /appli/src/python --rm --name pyenv pythonbuild bash"
    },
    "extra": {
        "github:kumar91gopi":"Algorithms-and-Data-Structures-in-Ruby",
        "github:TheAlgorithms":"Python",
        "python": "docker exec -it pyenv /bin/bash"
    }
}
